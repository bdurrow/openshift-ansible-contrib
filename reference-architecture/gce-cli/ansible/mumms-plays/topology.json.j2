{
  "clusters": [
{%- set clusters = {} -%}
{%- for node in glusterfs_nodes -%}
  {%- set cluster = hostvars[node].glusterfs_cluster if 'glusterfs_cluster' in node else '1' -%}
  {%- if cluster in clusters -%}
    {%- set _dummy = clusters[cluster].append(node) -%}
  {%- else -%}
    {%- set _dummy = clusters.update({cluster: [ node, ]}) -%}
  {%- endif -%}
{%- endfor -%}
{%- for cluster in clusters -%}
    {
      "nodes": [
{%- for node in clusters[cluster] -%}
        {
          "node": {
            "hostnames": {
              "manage": [
                "{{ hostvars[node].glusterfs_hostname | default(node) }}"
              ],
              "storage": [
                "{{ hostvars[node].glusterfs_ip | default(hostvars[node].gce_private_ip) }}"
              ]
            },
            "zone": {{ hostvars[node].glusterfs_zone | default(zone_to_number[hostvars[node].gce_zone]) | default(1) }}
          },
          "devices": [
{%- for device in glusterfs_devices -%}
            "{{ device }}"{% if not loop.last %},{% endif %}
{%- endfor -%}
          ]
        }{% if not loop.last %},{% endif %}
{%- endfor -%}
      ]
    }{% if not loop.last %},{% endif %}
{%- endfor -%}
  ]
}
